import 'package:flutter/material.dart';

class airVentureTheme{
  static ThemeData lightheme = ThemeData(
    brightness: Brightness.light,
    textTheme: TextTheme(

    )
  );
  static ThemeData darktheme = ThemeData(
    brightness: Brightness.dark
  );
}